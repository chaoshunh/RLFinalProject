function X = randomTest(C,N)

% 2 ways, by N steps, by a tuple with 3 entries
% the tuple = (cluster index, pr, nr), where pr is the reward returned if
% it is chosen, otherwise nr is returned

X = zeros(C,N,3); 

for n = 1:N
    clus
    for c = 1:C
        X(c,n) = [];
    end
end


